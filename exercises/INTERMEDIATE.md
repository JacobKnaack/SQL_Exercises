# Intermediate SQL Exercises

Sample Tables

```sql

```

Seeding Table Data

```sql

```

## Aggregate + Grouping

* Total revenue per day
* Average order size per user
* Top 5 customers by spend
* Count of order per product query

### Add Complexity

* Filter groups with `HAVING`
* Combine multiple aggregates in one query

## Subquery Challenges

* Find users who spent more than the average user
* find products priced above the category average
* Get the most recent order per user
* Get the top 3 order by value per user

## Window Function Drills

* Running total per user
* Rank users by monthly spend
* Compare each order to the users's average order size
* Find the previous order data per user
